## Use case

<The trigger, as one sentence: "when <situation>, an engineer must <outcome>".>

## How it is done today

<The steps taken now, which stages or skills are used or skipped, and where the current lifecycle gets in the way.>

## What the workflow should provide

<Artifacts, gates and checks the case needs; existing ones that do not fit and why.>

## Playbook or package workflow

<Whether a repository playbook (docs/playbooks/) would cover it, or why it needs a package skill (new gates, artifacts or hooks).>

## Environment

<Verbatim output of `python3 scripts/aidlc.py report-context`.>

## Example

<One real occurrence, described without customer data or ticket contents; `not observed` when there is none yet.>
